# pedroXIT

Site de divulgação do servidor do Discord.

## Como visualizar

Abra o arquivo `index.html` em um navegador ou rode um servidor local simples:

```bash
python -m http.server 8000
```

Depois, acesse `http://localhost:8000` no navegador.
